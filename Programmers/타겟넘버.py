#깊이우선탐색
# 배열의 값을 더하거나 빼서 t값이 되는 개수
def solution(numbers, target):
    res = []
    for i in range(len(numbers)):
        for j in range(i,len(numbers))
        res.append(i+j)



n = [1,1,1,1,1]
t = 3
print(solution(n,t))